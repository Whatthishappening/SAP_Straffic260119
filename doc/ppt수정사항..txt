6. ERD ppt 수정사항

특히 데이터의 30분 단위로 업데이트하여, 
>> 스케줄링을 통해 30분 단위로 프로그램을 실행하여


시설에 문제가 생기면 해당 "보관함" 정보와 즉시 매칭되어 웹에 반영됩니다.
>> 시설에 문제가 생기면 해당 "역" 정보와 즉시 매칭되어 웹에 반영됩니다.


세번째, 이 이슈를 해결할 관리자(user) 정보를 연결했습니다.
누가, 언제, 어떤 문제를 해결해야 하는지 알기 위함입니다.
>> 회원정보를 관리하는 User 테이블 입니다.
성함 ID PW 관리 역 정보 등 개인 정보를 관리하는 테이블입니다.


마지막으로, 날씨(weather)와 시설 정보(storage)는 지원 데이터로 배치했습니다.
메인 흐름과는 별도로 관리자가 현장 상황을 구체적으로 판단할 수 있게 도와주는 역할을 합니다.
>>마지막으로 날씨 테이블입니다. 
기상청API를 통해 정보를 받아와 DB에 저장하고 대시보드에 표출됩니다.

